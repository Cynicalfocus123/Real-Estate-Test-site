import { Router } from "express";
import multer from "multer";
import type { PoolConnection, ResultSetHeader, RowDataPacket } from "mysql2/promise";
import { z } from "zod";
import { executeSql, queryRows, withTransaction } from "../db/pool";
import { requireAuth, requireOneOfRoles, requireRole } from "../middleware/auth";
import { requireSameOrigin } from "../middleware/csrf";
import { deleteListingImageFiles, saveListingImages } from "../services/imageService";
import { ApiError } from "../utils/errors";
import { sanitizeEmail, sanitizeHttpUrl, sanitizePlainText, sanitizeSlug, sanitizeStringArray } from "../utils/sanitize";

export const PROPERTY_TYPES = ["VILLA", "CONDO", "APARTMENT", "TOWNHOUSE", "COMMERCIAL_BUILDING", "RESORT", "LAND", "HOUSE", "MULTI_FAMILY", "SINGLE_DETACHED_HOUSE", "SEMI_DETACHED_HOUSE"] as const;
export const VIEW_TYPES = ["BEACH", "RURAL", "MOUNTAIN", "LAKE", "WATERFALL", "CITY"] as const;
export const NEARBY_TYPES = ["HOSPITAL", "SCHOOL", "AIRPORT", "SHOPPING_MALL", "BEACH", "TRANSPORTATION", "CITY"] as const;
export const SPECIAL_CATEGORIES = ["DISTRESS_PROPERTY", "FORECLOSURE", "PRE_FORECLOSURE", "FIXER_UPPER", "URGENT_SALE", "FEATURED", "NEW_LISTING"] as const;
export const LISTING_STATUSES = ["DRAFT", "PUBLISHED", "ARCHIVED", "DELETED"] as const;
const upload = multer({ storage: multer.memoryStorage(), limits: { files: 12, fileSize: 8 * 1024 * 1024 } });
const array = z.array(z.string().min(1).max(240)).max(100).default([]);
const faq = z.object({ id: z.number().int().positive().optional(), question: z.string().min(1).max(240), answer: z.string().min(1).max(5000), isActive: z.boolean().default(true) });
const nearby = z.object({ id: z.number().int().positive().optional(), locationType: z.enum(NEARBY_TYPES), name: z.string().min(1).max(180), distanceLabel: z.string().min(1).max(80), distanceMeters: z.number().nonnegative().nullable().optional(), sortOrder: z.number().int().nonnegative().default(0) });
const senior = z.object({ roomSize: z.number().nonnegative().nullable().optional(), buildingSize: z.number().nonnegative().nullable().optional(), caregiverIncluded: z.boolean().nullable().optional(), caregiverNotes: z.string().max(5000).nullable().optional(), seniorCareService: z.string().max(160).nullable().optional(), serviceDuration: z.string().max(80).nullable().optional(), serviceDeposit: z.number().nonnegative().nullable().optional(), monthlyServiceFee: z.number().nonnegative().nullable().optional(), servicesIncluded: array, seniorPropertyFeatures: array, communityAmenities: array });
const seo = z.object({ seoTitle: z.string().max(180).nullable().optional(), metaDescription: z.string().max(320).nullable().optional(), canonicalUrl: z.string().max(500).nullable().optional(), indexStatus: z.enum(["index", "noindex"]).default("index"), followStatus: z.enum(["follow", "nofollow"]).default("follow"), ogTitle: z.string().max(180).nullable().optional(), ogDescription: z.string().max(320).nullable().optional(), ogImage: z.string().max(500).nullable().optional(), twitterTitle: z.string().max(180).nullable().optional(), twitterDescription: z.string().max(320).nullable().optional(), twitterImage: z.string().max(500).nullable().optional(), schemaType: z.string().max(80).default("RealEstateListing") });
export const propertyPayloadSchema = z.object({
  title: z.string().min(1).max(180), slug: z.string().max(180).optional(), transactionMode: z.enum(["SALE", "RENT"]), listingChannel: z.enum(["STANDARD", "SENIOR_HOME"]), publicStatusLabel: z.string().max(120).nullable().optional(), normalizedPropertyType: z.enum(PROPERTY_TYPES).nullable().optional(), specialCategory: z.enum(SPECIAL_CATEGORIES).nullable().optional(), propertyCondition: z.string().max(80).nullable().optional(), conditionLabel: z.string().max(120).nullable().optional(), viewType: z.enum(VIEW_TYPES).nullable().optional(), status: z.enum(LISTING_STATUSES).default("DRAFT"),
  priceAmount: z.number().nonnegative().nullable().optional(), buyPrice: z.number().nonnegative().nullable().optional(), rentMonthlyPrice: z.number().nonnegative().nullable().optional(), depositAmount: z.number().nonnegative().nullable().optional(), depositMonths: z.number().nonnegative().nullable().optional(), downPaymentAmount: z.number().nonnegative().nullable().optional(), mortgageTerm: z.string().max(80).nullable().optional(), mortgageInterestRate: z.number().nonnegative().nullable().optional(), estimatedMonthlyMortgage: z.number().nonnegative().nullable().optional(), currencyCode: z.string().min(3).max(12).default("THB"), priceUnitLabel: z.string().max(80).nullable().optional(), description: z.string().max(10000).nullable().optional(), highlights: array, amenities: array, features: array, propertyDetails: array,
  furnishingStatus: z.string().max(80).nullable().optional(), hasAirConditioner: z.boolean().nullable().optional(), hasKitchen: z.boolean().nullable().optional(), bedrooms: z.number().int().min(0).max(100).nullable().optional(), bathrooms: z.number().int().min(0).max(100).nullable().optional(), landSize: z.number().nonnegative().nullable().optional(), interiorSizeSqm: z.number().nonnegative().nullable().optional(), builtYear: z.number().int().min(1800).max(2200).nullable().optional(), floorCount: z.number().int().min(0).max(300).nullable().optional(), garageSpaces: z.number().int().min(0).max(100).nullable().optional(),
  country: z.string().max(120).default("Thailand"), province: z.string().max(120).nullable().optional(), amphoe: z.string().max(120).nullable().optional(), district: z.string().max(120).nullable().optional(), tambon: z.string().max(120).nullable().optional(), city: z.string().max(120).nullable().optional(), village: z.string().max(160).nullable().optional(), soi: z.string().max(160).nullable().optional(), streetAddress: z.string().max(240).nullable().optional(), postalCode: z.string().max(20).nullable().optional(), latitude: z.number().min(-90).max(90).nullable().optional(), longitude: z.number().min(-180).max(180).nullable().optional(), mapSearchLabel: z.string().max(255).nullable().optional(), agentId: z.number().int().positive().nullable().optional(), faqs: z.array(faq).max(50).default([]), nearbyLocations: z.array(nearby).max(50).default([]), seniorDetails: senior.nullable().optional(), seo: seo.default({}),
});
type Payload = z.infer<typeof propertyPayloadSchema>; type Row = RowDataPacket & Record<string, unknown>;
const num = (value: unknown) => value === null || value === undefined ? null : Number(value);
const bool = (value: unknown) => value === null || value === undefined ? null : Boolean(value);
const json = (value: unknown) => { try { const parsed = typeof value === "string" ? JSON.parse(value) : value; return Array.isArray(parsed) ? parsed.filter((item): item is string => typeof item === "string") : []; } catch { return []; } };
const text = (value: string | null | undefined, max: number) => value ? sanitizePlainText(value, max) : null;
function safeCanonical(value: string | null | undefined) { if (!value) return null; const result = sanitizeHttpUrl(value, 500); if (!result || !result.startsWith("https://buyhomeforless.com/")) throw new ApiError(400, "Canonical URL must use the approved public origin"); return result; }
function safeImageRef(value: string | null | undefined) { if (!value) return null; const result = sanitizeHttpUrl(value, 500); if (!result || !result.startsWith("https://buyhomeforless.com/uploads/")) throw new ApiError(400, "Image URL must use the approved upload origin"); return result; }
export function propertyDisplayPrice(property: { transactionMode: "SALE" | "RENT"; buyPrice?: number | null; rentMonthlyPrice?: number | null; priceAmount?: number | null; currencyCode?: string }) { const amount = property.transactionMode === "RENT" ? property.rentMonthlyPrice : property.buyPrice ?? property.priceAmount; return amount === null || amount === undefined ? "Price on request" : `${property.currencyCode ?? "THB"} ${Number(amount).toLocaleString()}`; }
function summary(row: Row) { return { id: Number(row.id), title: String(row.title), slug: String(row.slug), transactionMode: row.transaction_mode, listingChannel: row.listing_channel, publicStatusLabel: row.public_status_label, status: row.status, normalizedPropertyType: row.normalized_property_type, specialCategory: row.special_category, province: row.province, city: row.city, priceAmount: num(row.price_amount), buyPrice: num(row.buy_price), rentMonthlyPrice: num(row.rent_monthly_price), currencyCode: String(row.currency_code), thumbnailUrl: row.thumbnail_url ?? null, updatedAt: row.updated_at, displayPrice: propertyDisplayPrice({ transactionMode: row.transaction_mode as "SALE" | "RENT", buyPrice: num(row.buy_price), rentMonthlyPrice: num(row.rent_monthly_price), priceAmount: num(row.price_amount), currencyCode: String(row.currency_code) }) }; }
async function details(id: number) { const rows = await queryRows<Row[]>("SELECT l.*, (SELECT card_url FROM listing_images WHERE listing_id=l.id ORDER BY is_cover DESC,sort_order,id LIMIT 1) thumbnail_url FROM listings l WHERE l.id=? LIMIT 1", [id]); const row = rows[0]; if (!row) throw new ApiError(404, "Property not found"); const [images, faqs, places, seniorRows, agents] = await Promise.all([queryRows<Row[]>("SELECT id,original_name,card_url,detail_url,gallery_url,alt_text,caption,sort_order,is_cover FROM listing_images WHERE listing_id=? ORDER BY sort_order,id", [id]), queryRows<Row[]>("SELECT id,question,answer,is_active,sort_order FROM listing_faqs WHERE listing_id=? ORDER BY sort_order,id", [id]), queryRows<Row[]>("SELECT id,location_type,name,distance_label,distance_meters,sort_order FROM listing_nearby_locations WHERE listing_id=? ORDER BY sort_order,id", [id]), queryRows<Row[]>("SELECT * FROM senior_details WHERE listing_id=?", [id]), queryRows<Row[]>("SELECT a.* FROM agents a JOIN listing_agent_assignments x ON x.agent_id=a.id WHERE x.listing_id=?", [id])]); const s = seniorRows[0]; const agent = agents[0]; return { ...summary(row), description: row.description, propertyCondition: row.property_condition, conditionLabel: row.condition_label, viewType: row.view_type, priceUnitLabel: row.price_unit_label, depositAmount: num(row.deposit_amount), depositMonths: num(row.deposit_months), downPaymentAmount: num(row.down_payment_amount), mortgageTerm: row.mortgage_term, mortgageInterestRate: num(row.mortgage_interest_rate), estimatedMonthlyMortgage: num(row.estimated_monthly_mortgage), highlights: json(row.highlights), amenities: json(row.amenities), features: json(row.features), propertyDetails: json(row.property_details), furnishingStatus: row.furnishing_status, hasAirConditioner: bool(row.has_air_conditioner), hasKitchen: bool(row.has_kitchen), bedrooms: num(row.bedrooms), bathrooms: num(row.bathrooms), landSize: num(row.land_size), interiorSizeSqm: num(row.interior_size_sqm), builtYear: num(row.built_year), floorCount: num(row.floor_count), garageSpaces: num(row.garage_spaces), location: { country: row.country, province: row.province, amphoe: row.amphoe, district: row.district, tambon: row.tambon, city: row.city, village: row.village, soi: row.soi, streetAddress: row.street_address, postalCode: row.postal_code, latitude: num(row.latitude), longitude: num(row.longitude), mapSearchLabel: row.map_search_label }, seo: { slug: row.slug, seoTitle: row.seo_title, metaDescription: row.meta_description, canonicalUrl: row.canonical_url, indexStatus: row.index_status, followStatus: row.follow_status, ogTitle: row.og_title, ogDescription: row.og_description, ogImage: row.og_image, twitterTitle: row.twitter_title, twitterDescription: row.twitter_description, twitterImage: row.twitter_image, schemaType: row.schema_type }, images: images.map((x) => ({ id: Number(x.id), originalName: x.original_name, cardUrl: x.card_url, detailUrl: x.detail_url, galleryUrl: x.gallery_url, altText: x.alt_text, caption: x.caption, sortOrder: num(x.sort_order), isCover: Boolean(x.is_cover) })), faqs: faqs.map((x) => ({ id: Number(x.id), question: x.question, answer: x.answer, isActive: Boolean(x.is_active), sortOrder: num(x.sort_order) })), nearbyLocations: places.map((x) => ({ id: Number(x.id), locationType: x.location_type, name: x.name, distanceLabel: x.distance_label, distanceMeters: num(x.distance_meters), sortOrder: num(x.sort_order) })), seniorDetails: s ? { roomSize: num(s.room_size), buildingSize: num(s.building_size), caregiverIncluded: bool(s.caregiver_included), caregiverNotes: s.caregiver_notes, seniorCareService: s.senior_care_service, serviceDuration: s.service_duration, serviceDeposit: num(s.service_deposit), monthlyServiceFee: num(s.monthly_service_fee), servicesIncluded: json(s.services_included), seniorPropertyFeatures: json(s.senior_property_features), communityAmenities: json(s.community_amenities) } : null, agent: agent ? { id: Number(agent.id), name: agent.name, phone: agent.phone, email: agent.email, agency: agent.agency, isActive: Boolean(agent.is_active), isVerified: bool(agent.is_verified) } : null }; }
async function relations(connection: PoolConnection, id: number, data: Payload) { await connection.query("DELETE FROM listing_faqs WHERE listing_id=?", [id]); for (const [index, item] of data.faqs.entries()) await connection.query("INSERT INTO listing_faqs (listing_id,question,answer,sort_order,is_active) VALUES (?,?,?,?,?)", [id, text(item.question, 240), text(item.answer, 5000), index, item.isActive ? 1 : 0]); await connection.query("DELETE FROM listing_nearby_locations WHERE listing_id=?", [id]); for (const [index, item] of data.nearbyLocations.entries()) await connection.query("INSERT INTO listing_nearby_locations (listing_id,location_type,name,distance_label,distance_meters,sort_order) VALUES (?,?,?,?,?,?)", [id, item.locationType, text(item.name, 180), text(item.distanceLabel, 80), item.distanceMeters ?? null, index]); if (data.agentId) { const [available] = await connection.query<RowDataPacket[]>("SELECT id FROM agents WHERE id=? AND is_active=1", [data.agentId]); if (!available.length) throw new ApiError(400, "Selected agent is unavailable"); await connection.query("INSERT INTO listing_agent_assignments (listing_id,agent_id,is_primary) VALUES (?,?,1) ON DUPLICATE KEY UPDATE agent_id=VALUES(agent_id),is_primary=1", [id, data.agentId]); } else await connection.query("DELETE FROM listing_agent_assignments WHERE listing_id=?", [id]); if (data.seniorDetails) { const x = data.seniorDetails; await connection.query("INSERT INTO senior_details (listing_id,room_size,building_size,caregiver_included,caregiver_notes,senior_care_service,service_duration,service_deposit,monthly_service_fee,services_included,senior_property_features,community_amenities) VALUES (?,?,?,?,?,?,?,?,?,?,?,?) ON DUPLICATE KEY UPDATE room_size=VALUES(room_size),building_size=VALUES(building_size),caregiver_included=VALUES(caregiver_included),caregiver_notes=VALUES(caregiver_notes),senior_care_service=VALUES(senior_care_service),service_duration=VALUES(service_duration),service_deposit=VALUES(service_deposit),monthly_service_fee=VALUES(monthly_service_fee),services_included=VALUES(services_included),senior_property_features=VALUES(senior_property_features),community_amenities=VALUES(community_amenities)", [id,x.roomSize??null,x.buildingSize??null,x.caregiverIncluded===null||x.caregiverIncluded===undefined?null:x.caregiverIncluded?1:0,text(x.caregiverNotes,5000),text(x.seniorCareService,160),text(x.serviceDuration,80),x.serviceDeposit??null,x.monthlyServiceFee??null,JSON.stringify(sanitizeStringArray(x.servicesIncluded,100,240)),JSON.stringify(sanitizeStringArray(x.seniorPropertyFeatures,100,240)),JSON.stringify(sanitizeStringArray(x.communityAmenities,100,240))]); } }
function values(data: Payload) { const x = data.seo; return [text(data.title,180),sanitizeSlug(data.slug??data.title),data.transactionMode === "RENT" ? "RENT" : "BUY",data.transactionMode,data.listingChannel,text(data.publicStatusLabel,120),data.normalizedPropertyType??null,data.specialCategory??null,text(data.propertyCondition,80),text(data.conditionLabel,120),data.viewType??null,data.status,data.specialCategory??"NEW_LISTING",data.priceAmount??null,data.buyPrice??null,data.rentMonthlyPrice??null,data.depositAmount??null,data.depositMonths??null,data.downPaymentAmount??null,text(data.mortgageTerm,80),data.mortgageInterestRate??null,data.estimatedMonthlyMortgage??null,text(data.currencyCode.toUpperCase(),12),text(data.priceUnitLabel,80),text(data.description,10000),JSON.stringify(sanitizeStringArray(data.highlights,100,240)),JSON.stringify(sanitizeStringArray(data.amenities,100,240)),JSON.stringify(sanitizeStringArray(data.features,100,240)),JSON.stringify(sanitizeStringArray(data.propertyDetails,100,240)),text(data.furnishingStatus,80),data.hasAirConditioner===null||data.hasAirConditioner===undefined?null:data.hasAirConditioner?1:0,data.hasKitchen===null||data.hasKitchen===undefined?null:data.hasKitchen?1:0,data.bedrooms??null,data.bathrooms??null,data.landSize??null,data.interiorSizeSqm??null,data.builtYear??null,data.floorCount??null,data.garageSpaces??null,text(data.streetAddress,240),text(data.village,160),text(data.soi,160),text(data.tambon,120),text(data.amphoe,120),text(data.district,120),text(data.city,120),text(data.province,120),text(data.postalCode,20),text(data.country,120)??"Thailand",data.latitude??null,data.longitude??null,text(data.mapSearchLabel,255),text(x.seoTitle,180),text(x.metaDescription,320),safeCanonical(x.canonicalUrl),x.indexStatus,x.followStatus,text(x.ogTitle,180),text(x.ogDescription,320),safeImageRef(x.ogImage),text(x.twitterTitle,180),text(x.twitterDescription,320),safeImageRef(x.twitterImage),text(x.schemaType,80)??"RealEstateListing"]; }
const columns = "title=?,slug=?,section=?,transaction_mode=?,listing_channel=?,public_status_label=?,normalized_property_type=?,special_category=?,property_condition=?,condition_label=?,view_type=?,status=?,category=?,price_amount=?,buy_price=?,rent_monthly_price=?,deposit_amount=?,deposit_months=?,down_payment_amount=?,mortgage_term=?,mortgage_interest_rate=?,estimated_monthly_mortgage=?,currency_code=?,price_unit_label=?,description=?,highlights=?,amenities=?,features=?,property_details=?,furnishing_status=?,has_air_conditioner=?,has_kitchen=?,bedrooms=?,bathrooms=?,land_size=?,interior_size_sqm=?,built_year=?,floor_count=?,garage_spaces=?,street_address=?,village=?,soi=?,tambon=?,amphoe=?,district=?,city=?,province=?,postal_code=?,country=?,latitude=?,longitude=?,map_search_label=?,seo_title=?,meta_description=?,canonical_url=?,index_status=?,follow_status=?,og_title=?,og_description=?,og_image=?,twitter_title=?,twitter_description=?,twitter_image=?,schema_type=?,updated_at=CURRENT_TIMESTAMP";
async function uniqueSlug(connection: PoolConnection, slug: string, except?: number) { const [rows] = await connection.query<RowDataPacket[]>(`SELECT id FROM listings WHERE slug=?${except ? " AND id<>?" : ""} LIMIT 1`, except ? [slug, except] : [slug]); if (rows.length) throw new ApiError(409, "This slug is already in use"); }
export const adminPropertyListQuerySchema = z.object({q:z.string().max(120).optional(),status:z.enum(LISTING_STATUSES).optional(),transactionMode:z.enum(["SALE","RENT"]).optional(),listingChannel:z.enum(["STANDARD","SENIOR_HOME"]).optional(),propertyType:z.enum(PROPERTY_TYPES).optional(),page:z.coerce.number().int().min(1).default(1),pageSize:z.coerce.number().int().min(1).max(100).default(20)});
export function buildAdminPropertyFilters(q: z.infer<typeof adminPropertyListQuerySchema>) { const where=["1=1"],p:unknown[]=[]; if(q.q){where.push("(l.title LIKE ? OR l.slug LIKE ? OR l.province LIKE ? OR l.city LIKE ?)");const s=`%${q.q}%`;p.push(s,s,s,s);} if(q.status){where.push("l.status=?");p.push(q.status);} else where.push("l.status<>'DELETED'"); if(q.transactionMode){where.push("l.transaction_mode=?");p.push(q.transactionMode);} if(q.listingChannel){where.push("l.listing_channel=?");p.push(q.listingChannel);} if(q.propertyType){where.push("l.normalized_property_type=?");p.push(q.propertyType);} return {where,p}; }
export const adminPropertyRoutes = Router(); adminPropertyRoutes.use(requireAuth, requireOneOfRoles(["HEAD_ADMIN", "ADMIN", "EMPLOYEE"]), requireSameOrigin);
adminPropertyRoutes.get("/properties", async (req,res,next) => { try { const q=adminPropertyListQuerySchema.parse(req.query); const {where,p}=buildAdminPropertyFilters(q); const count=await queryRows<Row[]>(`SELECT COUNT(*) total FROM listings l WHERE ${where.join(" AND ")}`,p), rows=await queryRows<Row[]>(`SELECT l.*, (SELECT card_url FROM listing_images WHERE listing_id=l.id ORDER BY is_cover DESC,sort_order,id LIMIT 1) thumbnail_url FROM listings l WHERE ${where.join(" AND ")} ORDER BY l.updated_at DESC LIMIT ? OFFSET ?`,[...p,q.pageSize,(q.page-1)*q.pageSize]); res.json({items:rows.map(summary),pagination:{page:q.page,pageSize:q.pageSize,total:num(count[0]?.total)??0,totalPages:Math.max(1,Math.ceil((num(count[0]?.total)??0)/q.pageSize))}}); } catch(e){next(e);} });
adminPropertyRoutes.get("/properties/:id", async (req,res,next)=>{try{res.json(await details(z.coerce.number().int().positive().parse(req.params.id)));}catch(e){next(e);}});
adminPropertyRoutes.post("/properties", async (req,res,next)=>{try{const data=propertyPayloadSchema.parse(req.body);const id=await withTransaction(async c=>{const slug=sanitizeSlug(data.slug??data.title);if(!slug)throw new ApiError(400,"Unable to generate slug");await uniqueSlug(c,slug);const [result]=await c.query<ResultSetHeader>(`INSERT INTO listings (${columns.replace(/=\?/g,"").replace(/,updated_at=CURRENT_TIMESTAMP/,"")},created_by) VALUES (${Array(64).fill("?").join(",")},?)`,[...values({...data,slug}),req.user!.id]);const id=Number(result.insertId);await relations(c,id,data);return id;});res.status(201).json({id,property:await details(id)});}catch(e){next(e);}});
adminPropertyRoutes.patch("/properties/:id", async (req,res,next)=>{try{const id=z.coerce.number().int().positive().parse(req.params.id);const existing=await details(id) as Record<string, unknown>;const existingLocation=(existing.location??{}) as Record<string, unknown>;const existingSeo=(existing.seo??{}) as Record<string, unknown>;const incoming=req.body as Record<string, unknown>;const data=propertyPayloadSchema.parse({...existing,...existingLocation,agentId:(existing.agent as {id?:number}|null)?.id??null,...incoming,seo:{...existingSeo,...(incoming.seo as Record<string,unknown>|undefined)}});await withTransaction(async c=>{const slug=sanitizeSlug(data.slug??data.title);if(!slug)throw new ApiError(400,"Unable to generate slug");await uniqueSlug(c,slug,id);const [result]=await c.query<ResultSetHeader>(`UPDATE listings SET ${columns} WHERE id=?`,[...values({...data,slug}),id]);if(!result.affectedRows)throw new ApiError(404,"Property not found");await relations(c,id,data);});res.json({id,property:await details(id)});}catch(e){next(e);}});
adminPropertyRoutes.delete("/properties/:id", async(req,res,next)=>{try{const id=z.coerce.number().int().positive().parse(req.params.id);const r=await executeSql("UPDATE listings SET status='DELETED',updated_at=CURRENT_TIMESTAMP WHERE id=?",[id]);if(!r.affectedRows)throw new ApiError(404,"Property not found");res.json({id,status:"DELETED"});}catch(e){next(e);}});
adminPropertyRoutes.post("/properties/:id/restore", async(req,res,next)=>{try{const id=z.coerce.number().int().positive().parse(req.params.id);const r=await executeSql("UPDATE listings SET status='DRAFT',updated_at=CURRENT_TIMESTAMP WHERE id=? AND status='DELETED'",[id]);if(!r.affectedRows)throw new ApiError(404,"Deleted property not found");res.json({id,status:"DRAFT"});}catch(e){next(e);}});
adminPropertyRoutes.post("/properties/:id/images", upload.array("images",12), async(req,res,next)=>{try{const id=z.coerce.number().int().positive().parse(req.params.id);res.status(201).json({items:await saveListingImages(id,(req.files as Express.Multer.File[]|undefined)??[])});}catch(e){next(e);}});
adminPropertyRoutes.patch("/properties/:id/images/:imageId", async(req,res,next)=>{try{const id=z.coerce.number().int().positive().parse(req.params.id),image=z.coerce.number().int().positive().parse(req.params.imageId),data=z.object({altText:z.string().max(180).nullable().optional(),caption:z.string().max(240).nullable().optional()}).parse(req.body),r=await executeSql("UPDATE listing_images SET alt_text=?,caption=? WHERE id=? AND listing_id=?",[text(data.altText,180),text(data.caption,240),image,id]);if(!r.affectedRows)throw new ApiError(404,"Image not found");res.json({id:image});}catch(e){next(e);}});
adminPropertyRoutes.patch("/properties/:id/images/reorder", async(req,res,next)=>{try{const id=z.coerce.number().int().positive().parse(req.params.id),ids=z.object({imageIds:z.array(z.number().int().positive()).max(12)}).parse(req.body).imageIds;const rows=await queryRows<Row[]>("SELECT id FROM listing_images WHERE listing_id=? ORDER BY sort_order,id",[id]);if(ids.length!==rows.length||new Set(ids).size!==ids.length||ids.some(x=>!rows.some(r=>Number(r.id)===x)))throw new ApiError(400,"Image order must contain every property image once");await withTransaction(async c=>{for(const [n,image] of ids.entries())await c.query("UPDATE listing_images SET sort_order=? WHERE id=? AND listing_id=?",[n,image,id]);});res.json({ok:true});}catch(e){next(e);}});
adminPropertyRoutes.patch("/properties/:id/images/:imageId/cover", async(req,res,next)=>{try{const id=z.coerce.number().int().positive().parse(req.params.id),image=z.coerce.number().int().positive().parse(req.params.imageId);await withTransaction(async c=>{const [found]=await c.query<RowDataPacket[]>("SELECT id FROM listing_images WHERE id=? AND listing_id=?",[image,id]);if(!found.length)throw new ApiError(404,"Image not found");await c.query("UPDATE listing_images SET is_cover=0 WHERE listing_id=?",[id]);await c.query("UPDATE listing_images SET is_cover=1 WHERE id=?",[image]);});res.json({ok:true});}catch(e){next(e);}});
adminPropertyRoutes.delete("/properties/:id/images/cover", async(req,res,next)=>{try{await executeSql("UPDATE listing_images SET is_cover=0 WHERE listing_id=?",[z.coerce.number().int().positive().parse(req.params.id)]);res.json({ok:true});}catch(e){next(e);}});
adminPropertyRoutes.delete("/properties/:id/images/:imageId", async(req,res,next)=>{try{const id=z.coerce.number().int().positive().parse(req.params.id),image=z.coerce.number().int().positive().parse(req.params.imageId);const rows=await queryRows<Row[]>("SELECT card_url,banner_url,detail_url,mobile_url,gallery_url FROM listing_images WHERE id=? AND listing_id=?",[image,id]);if(!rows[0])throw new ApiError(404,"Image not found");await executeSql("DELETE FROM listing_images WHERE id=? AND listing_id=?",[image,id]);await deleteListingImageFiles([String(rows[0].card_url),String(rows[0].banner_url),String(rows[0].detail_url),String(rows[0].mobile_url),String(rows[0].gallery_url)]);res.json({ok:true});}catch(e){next(e);}});
adminPropertyRoutes.get("/agents",async(_q,res,next)=>{try{const rows=await queryRows<Row[]>("SELECT id,name,phone,email,agency,is_active,is_verified FROM agents ORDER BY is_active DESC,name");res.json({items:rows.map(x=>({id:Number(x.id),name:x.name,phone:x.phone,email:x.email,agency:x.agency,isActive:Boolean(x.is_active),isVerified:bool(x.is_verified)}))});}catch(e){next(e);}});
adminPropertyRoutes.post("/agents",requireOneOfRoles(["HEAD_ADMIN","ADMIN"]),async(req,res,next)=>{try{const x=z.object({name:z.string().min(1).max(120),phone:z.string().min(4).max(40),email:z.string().email(),agency:z.string().max(160).nullable().optional()}).parse(req.body),r=await executeSql("INSERT INTO agents (name,phone,email,agency) VALUES (?,?,?,?)",[text(x.name,120),text(x.phone,40),sanitizeEmail(x.email),text(x.agency,160)]);res.status(201).json({id:Number(r.insertId)});}catch(e){next(e);}});
adminPropertyRoutes.patch("/agents/:id",requireRole("HEAD_ADMIN"),async(req,res,next)=>{try{const id=z.coerce.number().int().positive().parse(req.params.id),x=z.object({name:z.string().min(1).max(120).optional(),phone:z.string().min(4).max(40).optional(),email:z.string().email().optional(),agency:z.string().max(160).nullable().optional(),isActive:z.boolean().optional(),isVerified:z.boolean().nullable().optional()}).parse(req.body),sets:string[]=[],p:unknown[]=[];for(const [key,column,max] of [["name","name",120],["phone","phone",40],["agency","agency",160]] as const)if(x[key]!==undefined){sets.push(`${column}=?`);p.push(text(x[key],max));}if(x.email){sets.push("email=?");p.push(sanitizeEmail(x.email));}if(x.isActive!==undefined){sets.push("is_active=?");p.push(x.isActive?1:0);}if(x.isVerified!==undefined){sets.push("is_verified=?");p.push(x.isVerified===null?null:x.isVerified?1:0);}if(!sets.length)throw new ApiError(400,"No agent changes supplied");const r=await executeSql(`UPDATE agents SET ${sets.join(",")},updated_at=CURRENT_TIMESTAMP WHERE id=?`,[...p,id]);if(!r.affectedRows)throw new ApiError(404,"Agent not found");res.json({id});}catch(e){next(e);}});
adminPropertyRoutes.delete("/agents/:id",requireRole("HEAD_ADMIN"),async(req,res,next)=>{try{const id=z.coerce.number().int().positive().parse(req.params.id);const r=await executeSql("DELETE FROM agents WHERE id=?",[id]);if(!r.affectedRows)throw new ApiError(404,"Agent not found");res.json({id});}catch(e){next(e);}});
